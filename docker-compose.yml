services:
  flask-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: zhdmra/flask-app:latest
    ports:
      - "5000:5000"
    volumes:
      - ./app:/app
    environment:
      - FLASK_ENV=development
      - FLASK_APP=app.py
    command: python app.py
